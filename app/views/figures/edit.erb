<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form class="" action="/figures/<%=@figure.id%>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">

      figure_name<input type="text" name="figure[name]" value="<%=@figure.name%>" placeholder="figure_name">
      <p>*****************TITLES**************</p>
      <br>

      <%@titles.each do |t|%>
        <li>
          <input type="checkbox" name="figure[title_ids][]" value="<%= t.id%>" placeholder="title_<%=t.id%>" <%= checked if @figure.titles.include?(t.id)%> >
          name:<%= t.name%> id:<%= t.id%>
        </li>
      <%end%>
      <p>OR CREATE NEW TITLE</p>
      name:<input type="text" name="title[name]" value="" placeholder="new_title"><br>

    </br>
      <p>*****************LANDMARKS*************</p>
      <%@landmarks.each do |l|%>
        <li>
          <input type="checkbox" name="figure[landmark_ids][]" value="<%= l.id%>" placeholder="landmark_<%=l.id%>">
          <%=l.name%> :: <%=l.year_completed%>
        </li>
      <%end%>
      <p>OR CREATE NEW LANDMARK</p>
      name:<input type="text" name="landmark[name]" value="" placeholder="new_landmark"><br>
      year_completed<input type="text" name="landmark[year_completed]" value="" placeholder="year_completed"><br>

    </br>
      <input type="submit" name="" value="Edit Figure">


    </form>
  </body>
</html>
